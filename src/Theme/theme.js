import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#ee2d2f',
            // light: '#ffffff',
            light: '#f7f4f2',
            initial: '#abbaab',
            final: '#ffffff',
            navNfo: 'linear-gradient(45deg, #0F2027, #203A43, #2C5364)'
        },
    }
})